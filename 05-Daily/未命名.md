1. Tabs 结构整改

（最大改动） - 设计稿是 8 个：全套活动 / 会场头图 / 会场组件 / 站内资源位 / 站外资源位 / 素材修改 / 图像处理 / 批量处理 - 当前是 6 个：自由模式 / 会场头图 / 会场组件 / 站内资源位 / 站外资源位 / C 端外素材 - 差异：「自由模式」→「全套活动」；「C 端外素材」删；新增「素材修改 / 图像处理 / 批量处理」

2. 创作面板从"自由输入"改成"结构化表单"

（最大功能差） - 设计稿是一组字段： - 提示句「生成一张会场头图」 - 渠道 + chip（外卖 APP） - 活动 + chip（神抢手） - 主标题 + 文字框 - 副标题 + 文字框 - 画面描述（必填） - 尺寸 宽度/高度（数字输入） - 当前是单一 textarea + 「自由模式 / 智能比例」切换 - 差异：整个面板要改版，字段结构全变

3. "AI 图像处理" section 整块删除

- 设计稿没有这节（5 个卡片不存在） - 当前有 AI延展 / AI抠图 ×3 / AI扩图 — 而且名字还重复 - 差异：这些能力可能挪到顶部「图像处理」tab 里了，首页首屏不再放

4. 模板区标题 + 布局

- 设计稿：没「全部模板」标题，直接密集网格，卡片多种比例混排（方形 / 横图 / 竖图）、带手机状态栏（20:11）和信号图标 → 呈现的是"手机上实际样子" - 当前：有「全部模板」标题 + 只 1 张 - 差异：去标题 + 网格支持多尺寸 + 模板卡片上加移动端 mockup 壳（状态栏 / 信号 / 电量）+ 模板数据量需要扩

5. Hero 区上下留白

- 设计稿：hero 文字离顶栏很近（~60px 左右） - 当前：hero 文字上方大量留白（视觉上 centered 在视口中） - 差异：需要压缩 hero 顶部 padding

6. 背景色调

- 设计稿：底部暖粉/橙渐变，整体偏暖 - 当前：整体偏冷灰 - 差异：渐变方向和颜色要调

7. 左侧栏

- 设计稿：4 个图标（home / 魔法棒 / 垃圾桶 / 田字格），

没有底部 N

头像

- 当前：相同 4 个 + 底部一个圆形「N」头像 - 差异：底部头像移除或挪位置

8. 顶栏右侧

- 设计稿和当前一致（头像 + 业务） - 无差异


| #   | 模块     | 组件              | 职责                                                                            |
| --- | ------ | --------------- | ----------------------------------------------------------------------------- |
| 1   | 卡片顶栏   | `EditorTopbar`  | 活动名面包屑 + 一键拓展 / 下载 / 关闭（`editor-shell.tsx:323`）                               |
| 2   | 左资源位面板 | `SlotPanel`     | 多尺寸 slot 列表（会场、延展尺寸），点击切换当前画布（`:336`）                                         |
| 3   | 中画布舞台  | `CanvasStage`   | 渲染所有图层、拖拽/吸附、画布内双击编辑文字、选中框（`:342`）                                            |
| 4   | 右属性面板  | `PropertyPanel` | 随选中状态切换："未选→画布尺寸 / 选模块→X Y W H+替换删除 / 选文字→位置+文字+填充色 / 选图片→位置+替换+不透明度"（`:352`） |

简单说就是 「顶栏 + 三栏（资源位 / 画布 / 属性）」 的 4 模块布局，再加两个按需弹窗。


大需求：原会场内容也支持 Figma

autolayout 拖拽换位

完整 5 阶段实施，目标：venue 画布上的原 PSD 内容（按图层组）和插入的会场组件统 一参与垂直自动布局，拖拽换位 + reflow 自动重排 + 删除自动收缩。

阶段 1：PSD 解析层支持

group（前置必做）

改

lib/psd-parser.ts

： 1. 解析时保留 ag-psd 返回的树形结构，不要打平 2. 把每个 group 节点转成

PsdLayer

记录： -

layerType: "group"

-

id

：group_ -

name

：PSD 里的 group 名 -

x / y / width / height

：所有子 layer 的 bbox union -

parentId

：指向父 group（顶层 group 为 null） -

zIndex

：按 PSD 中的 z 顺序 3. 子 layer 的

parentId

指向所属 group 的新 id 4. 多层嵌套 group 全部保留（A → B → C 三层嵌套链路完整） 5. 解析结果数组顺序：父 group 在前，子 layer 紧随其后（保证渲染时 z 序正确）

改

lib/venue-component-psd.ts

的

ensureRootGroup

： - 检测：如果 PSD 已有顶层 group，不再额外加根 group（保留 PSD 原始结构） - 否则按现状合成虚拟根 group（保留对扁平 PSD 的兼容）

后台「解析结果」UI（图 15 那个列表）改

components/admin/psd-manager.tsx

： - 按 parentId 渲染缩进树（group 用 ChevronDown icon + 16px 缩进 per level） - group 行可折叠/展开 - 解析成功 banner 加一句：「检测到 N 个图层组，编辑器中可整组拖拽换位」 - 没有 group 时显示警告：「该 PSD 未使用图层组，编辑器中只能拖拽插入的会 场组件，原内容只能自由拖动」

数据库迁移策略

： - 不写一次性脚本 - 旧上传的 PSD 模板（包括当前 demo 用的 venue 38 礼遇）由我手动重新上传，触发新解析逻辑 - 只迁移 demo 用到的那一条，其他历史数据 demo 后再说

阶段 2：编辑器引入 Block 抽象

新建

components/editor/venue-blocks.ts

：

export

type

Block = | {

kind

:

"originalGroup"

; groupId:

string

; layers: PsdLayer[]; sortKey:

number

} | {

kind

:

"instance"

; instanceId:

string

; layers: PsdLayer[]; sortKey:

number

} | {

kind

:

"loose"

; layers: PsdLayer[] };

// 不参与 reorder

export

function

extractBlocks(layers: PsdLayer[], canvasWidth:

number

,

canvasHeight:

number

): Block[] {

// 1. 顶层 group（parentId == null &&

layerType == "group"）→ originalGroup

// 其所有后代 layer 一起归入

layers 字段

// sortKey = 该 group 在 layers

数组里的下标

// 2. 有 instanceId 的 layer 按

instanceId 聚合 → instance

// sortKey = 该 instance 第一个

layer 在 layers 数组里的下标

// 3. 剩余 parentId == null 的非

group 非 instance leaf → loose

// 4. 排除铺底背景（复用

isFullCanvasBackground）

// 5. 嵌套子 group 跟着父 group

走，不独立成 block

}

关键规则

： -

loose

block 在 reflow 时保持原始 y 不变，作为「锚点」 -

originalGroup

和

instance

按

sortKey

排序统一参与 reorder - 同一 sortKey 表示插入顺序 / PSD 中的相对位置

阶段 3：reflow 算法升级

改

components/editor/insert-venue-compo

nent.ts

的 reflow 函数

（或抽到新文件

venue-reflow.ts

）：

function

reflowVenueBlocks( layers: PsdLayer[], editState: EditState, canvasWidth:

number

,

originalCanvasHeight:

number

): { layerUpdates:

Map

<

string

,

number

>; nextHeight:

number

} {

const

blocks = extractBlocks(layers, canvasWidth, originalCanvasHeight);

const

reorderable = blocks.filter(b => b.kind !==

"loose"

).sort(

(

a, b) => a.sortKey - b.sortKey);

const

looseLayers = blocks.filter(b => b.kind ===

"loose"

).flatMap(b => b.layers);

let

cursor = TOP_PADDING;

// 24

const

updates =

new

Map

<

string

,

number

>();

for

(

const

block

of

reorderable) {

const

visible = block.layers.filter(l => isVisible(l, editState));

if

(visible.length ===

0

)

continue

;

const

minY =

Math

.min(...visible.map(l => l.y));

const

maxBottom =

Math

.max(...visible.map(l => l.y + l.height));

const

dy = cursor - minY;

// 整个 block 所有 layer 一起平移

dy

block.layers.forEach(l => updates.set(l.id, l.y + dy)); cursor += (maxBottom - minY) + GAP;

// GAP = 24

}

// loose 不动，但参与高度计算

const

looseBottom = looseLayers.length >

0

?

Math

.max(...looseLayers.map(l => l.y + l.height)) :

0

;

const

ABSOLUTE_MIN =

200

;

const

nextHeight =

Math

.max(cursor, looseBottom, ABSOLUTE_MIN) +

48

;

return

{

layerUpdates

: updates, nextHeight }; }

触发

：editor-shell 的

useEffect([layers, editState])

调 reflow → 更新 layers y → 更新 venue slot.height。短路防无限循环（reflow 结果跟当前 layers 一致就不 set）。

阶段 4：拖拽换位扩展到所有 block

改

components/editor/canvas-stage.tsx

： 之前的拖拽换位逻辑（DragState 的

venue-instance

变体）扩展为

venue-block

： 1. 命中点击的 layer 后，向上追溯找到其所属的 block（originalGroup 或 instance） 2. dragstart 时快照其他所有 reorderable block 的

{ top, bottom, centerY,

sortKey }

3. dragmove 时只让被拖 block 的所有 layer 跟随 cursor 平移（x 锁定，y 受约束） 4. 计算

dropIndex

用 block 中心 y vs 其他 block 中点 5. dragend 时调

onReorderBlock(blockKey,

newIndex)

，shell 处理后续

改

components/editor/editor-shell.tsx

：

新增

handleReorderBlock(blockKey: {

kind, id }, newIndex)

： - 复用

reorderInstanceInLayers

的思路扩展为

reorderBlockInLayers

- 抽出被拖 block 的所有 layer，splice 到 layers 数组的新位置 - 注意保持 loose layer 的相对顺序不变 - setLayers 触发 reflow 自动重排

约束

： -

loose

block 不能被拖（点击 loose layer 时走原 module/element 自由拖拽逻辑，不进 block 拖拽分支） - 铺底背景同样不能拖（被 isFullCanvasBackground 排除） - 嵌套子 group 不能独立拖（点击子 group layer 时上溯找到顶层 group block）

阶段 5：撤销栈 + UI 提示

撤销栈

： - reorder block 操作进 undo 栈（layers 数组顺序变更纳入快照） - 之前 PR2 已确认快照结构是

{

editState, layers, venueHeight

}

，扩展到这里天然兼容

UI 提示

： - 拖拽期间显示 dropIndicator（蓝色 2px 横线在目标插入位置）—— PR2 已实现，复用即可 - 鼠标悬停在 originalGroup block 上时，显示淡蓝色 outline 提示「可拖拽换位」 - 同样的 outline 用在 instance 上保持一致

验收（必须录屏给我看）

1. 我手动重新上传 venue 38 礼遇 PSD → 解析结果列表显示嵌套 group 树状结构 ✓ 2. 进编辑器 → 原会场内容按 group 显示为可拖拽块 ✓ 3. 拖动「1对1急送」原 group 到「大额券限时领」上方 → 顺序对调，画布高度不变 ✓ 4. 插入 1 个新组件 → 拖动到原会场内容中间 → reorder 生效 ✓ 5. 原 group 和 instance 混合排序 → 拖拽 / 删除 / undo 全链路正常 ✓ 6. 删除中间任意 block → 下方 block 自动上移，画布收缩 ✓ 7. 铺底背景 / loose layer 不参与拖拽，行为不变 ✓ 8. 导出 PNG → 字体精确，所有 block 按当前顺序合成 ✓ 9.

npx tsc --noEmit

+

npm run lint

通过

风险与对策

风险:

ag-psd 嵌套 group 结构复杂

对策:

阶段 1 先打日志确认 venue PSD 实际嵌套深度，超 3 层 group 简化为只取顶层 ─────────────────────────────────────── ─

风险:

editState 里指向旧 id 的 entry 在重新解析后变孤儿

对策:

重新上传 = DB 全新记录 = editState 同步重置（用户重新打开编辑器即可） ─────────────────────────────────────── ─

风险:

reflow 把 loose layer 也推走

对策:

严格区分 loose / reorderable，loose 永远保留原 y ─────────────────────────────────────── ─

风险:

嵌套子 group 被误识别为顶层 block

对策:

extractBlocks 严格按

parentId ==

null

判定 ─────────────────────────────────────── ─

风险:

画布高度抖动

对策:

reflow 短路 + 拖拽过程不触发 reflow（PR2 已确认机制）

工作量与排期

总计 1.5-2 天紧凑实施。建议拆 commit： - commit 1：阶段 1（PSD 解析 + 后台 UI） - commit 2：阶段 2-3（block 抽象 + reflow） - commit 3：阶段 4（拖拽换位扩展） - commit 4：阶段 5（undo + UI 提示） 阶段 1 完成后立刻发给我，我手动重传 venue PSD 验证解析结构正确，再继续阶段 2。 开始。


