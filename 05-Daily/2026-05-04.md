---
tags: [日报]
date: 2026-05-04
---

# 2026-05-04

## 今日做了什么

Lollipop（Tian 前端分支）一天两波视觉迭代收尾：凌晨清掉 MiSans 字体包 + 多页视觉对齐 + AI 简历面板交互重做；下午把面试间 loading 屏从黑底单条进度改成白底 4 步 stepper。

1. **AI 简历优化面板（resume-ai）改造**：chip / 气泡视觉重做、输入态合并到一个外层容器（外 24 / 内 12 圆角同心）、长串字符溢出修复。AIInputBox 主改（+209 行）。
2. **多页视觉对齐 + 字体系统栈替换**：DESIGN.md 三轮基础上又一轮修订，history / leaderboard / resume-edit / interview 等页面 sweep；同步**移除 MiSans npm 包**（删 6 个 .css 字重文件 + package.json 依赖），改系统字体栈。删除已弃用的 setup 页（380 行）和 jobs/settings 页（228 行），新增 JdDetailModal（473 行）。
3. **面试间 loading 屏重做**：黑底单进度条 → 白底 4 步 stepper（摄像头/麦克风/AI/就绪），主界面背景 white→#F6F6F6，AI 字幕卡 / 视频卡 flex 比例 55:45 → 1:1，结束按钮换具体色值。

## 踩过的坑

- commit 流程里 Bash 工具连续 3 次都加 `cd frontend &&` 前缀，第二次开始 cwd 已经在 frontend 又 cd 一次变成 `frontend/frontend` 报错；最后改用绝对路径一次成功。已沉淀到全局 CLAUDE.md「Bash 工具 cwd 跨调用持久，不要无脑加 cd 前缀」。

## 教训

- MiSans npm 包字重 bug 的延续决策：与其在 lib/fonts 里 patch，不如直接换系统字体栈，从根上消掉 fontWeight: 550 渲染异常的整条问题链。
- 工具的描述文档要先读：cwd 持久那条 Bash tool 描述里就明写了，犯错纯粹是我没遵守。

## 下次接续

- 面试间新 loading 屏在真实网络下走一遍，验证 4 步 stepper 在慢速加载下时序是否合理
- DESIGN.md 第 N 轮修订之后是否还需要再 sweep 几个边角页面

## 资源

- 项目：`~/Dev/Lollipop_Test_Tian`
- 项目笔记：`~/Brain/01-Projects/Lollipop_Test_Tian.md`
- 今日 commits：4ccc348 / 3b1f349 / 6ddee5e
