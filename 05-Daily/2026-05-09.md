---
tags: [日报]
date: 2026-05-09
---

# 2026-05-09

## 今日做了什么

主题：DESIGN.md 1160 行大体检 —— 调研 6 家大厂规范、出 20 条诊断报告，今天 0 commit、文档未动，明天才动手改。顺手清掉两件小事。

1. **简历 AI 侧边栏：用户气泡上方加字段卡片 chip**：原来用户发完消息只显示文本气泡，看不出"针对哪个字段"。改成在分组首条用户消息气泡上方显示完整 chip（icon + label + 内容预览），视觉对齐 AIInputBox 上方的 ContextChip。改 `ResumeAIContext.tsx`（AIMessage 加 fieldContext 快照字段，sendMessage 时锁存 label + 36 字预览）+ `AIChatThread.tsx`（去掉旧 FieldChip，新增 UserFieldChip）。typecheck / lint baseline 全过。

2. **管理后台账号创建**：跑 `backend/reset_admin_password.py`，admin@lollipop.ai / admin1234 落地本地 Docker postgres。开发简易密码，登进去再改。

3. **DESIGN.md 大体检（主线，今天的大头）**：
   - **调研**：fork 一个 research agent 跑 633 秒，覆盖 Material 3 / Apple HIG / Carbon / Polaris / Primer / Atlassian 6 家大厂 + W3C DTCG / Keep a Changelog / WCAG 2.2 / Primer A11y 4 份权威规范，全部带 URL 出处
   - **通读 1160 行 DESIGN.md**：自己同步通读 + design-audit.md 派生审计文件
   - **出 20 条诊断**：分三档 —— 必须改 3 / 应该改 8 / 顺手改 9
   - **用户审阅后又用大白话重写了一版**（用户反馈技术色太重）
   - **关键发现**：
     - `--lp-brand-blue` 命名误导（实际值 `#191919` 黑色），违反 W3C DTCG / Atlassian 命名规范
     - 修订 (o) 删了 focus ring，违反 WCAG 2.2 SC 2.4.7 + 2.4.11（焦点态必须可见、对比 ≥ 3:1）
     - `#999` 在白底对比度仅 2.85:1，不过 WCAG AA 4.5:1（用作 Metadata / Hint / Footnote 是 fail）
     - §11 changelog 17 条修订单条 600+ 字塞文末，违反 Keep a Changelog 1.0 规范（应独立文件 + 短条目）
     - token 没分 primitive / semantic 两层，未来 dark mode / 换主题代价高

## 踩过的坑

- `exec(open(file).read())` 跑 Python 脚本时 `__file__` 在 exec 上下文里不存在 → 报 NameError。改回直接 `python file.py` 最稳。
- Bash cwd 跨调用持久那条规则今天又苗头了一下：lint 验证 stash 前后状态时差点 `cd frontend` 两次。及时刹住，全程用绝对路径。

## 教训

- **调研型任务一定 fork**。今天把"上网搜大厂规范"fork 给 research agent 跑了 10 分钟，自己同步通读 DESIGN.md，回收时直接对照 —— 比顺序串行好得多，noise 也不污染主上下文。
- **写日报 / 写诊断时主动核对调性**。第一版诊断用了"严重 / 架构 / 卫生"分级、夹了 emoji 和英文术语 —— 用户立刻反馈嫌技术色重。规范类输出要看读者，不是看自己。

## 下次接续

明天主线：开始动手改 DESIGN.md。按性价比排，先做这三件（每件都是一天内能做完的）：

1. **拆 changelog 出去**（半天）—— 新建 `frontend/CHANGELOG.md`，§11 那 17 条修订搬过去并按 [Keep a Changelog](https://keepachangelog.com/en/1.0.0/) 精简成短条目（每条 1-3 行，分 Added/Changed/Removed），DESIGN.md 文末只留指向链接
2. **加 Accessibility 顶级章节 + 还原 focus 态**（半天到一天）—— 不是恢复 3px ring，用克制版 `:focus-visible`（1.5px outline + 2px offset）过 WCAG 2.2
3. **rename `--lp-brand-blue` → `--lp-accent`**（半天）—— 整代码 sweep 替换，留兼容期

中期（按性价比往下）：
- token 分两层（primitive 色阶 + semantic）+ 抽 §Tokens 顶级章节
- `#999` 调深到 `#767676`（对比 4.54:1 过线）或限定"仅装饰文本"
- 组件章节加 Carbon 四件套（Usage / Style / Code / Accessibility）+ 状态完整列举（default/hover/active/focus/disabled/loading/error 七态）
- §10 Migration Plan 整段删（迁移待办挪 Issue）
- 加 §Patterns 章节（登录态 / 空状态 / 加载 / Onboarding）
- 加 §Content 章节（人称 / 错误提示语气 / CTA 动词）
- §8 集中 Don't 列表拆散到各章节（Material 推荐就近）

卫生层：TOC、Contributing、prefers-reduced-motion、Responsive 章节补组件 mobile 变体。

明天直接打开这些参考资料对照写：
- Material 3 Foundations: https://m3.material.io/foundations
- Carbon Component Checklist: https://carbondesignsystem.com/contributing/component-checklist/
- Atlassian Tokens: https://atlassian.design/foundations/tokens/design-tokens
- W3C DTCG: https://www.designtokens.org/tr/drafts/format/
- WCAG 2.2: https://www.w3.org/TR/WCAG22/
- Keep a Changelog 1.0: https://keepachangelog.com/en/1.0.0/
- Primer A11y: https://primer.style/accessibility/introduction/

## commit 流水

今天 0 commit。今天主线是审查不是改代码（用户要求"不要修改任何"）。working tree 还有一批 frontend 改动是 5/8 的尾巴 + 今天简历 AI 侧栏 chip 改动（`ResumeAIContext.tsx` + `AIChatThread.tsx`），未提交。

## 资源

- 项目：`/Users/tianruian/Dev/Lollipop_Test_Tian`
- 项目笔记：`~/Brain/01-Projects/Lollipop_Test_Tian.md`
