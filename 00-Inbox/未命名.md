|   |   |   |
|---|---|---|
|新会话自动读项目 CLAUDE.md + Obsidian 笔记 + `git status`|**Hook**（SessionStart）|每次都必须执行，不能靠 Claude 记得|
|超过 3 步自动进 Plan Mode|**CLAUDE.md 规则**|语义判断，Hook 做不到|
|改完代码自动跑 `tsc --noEmit` + lint|**Hook**（PostToolUse on Edit/Write）|最适合 Hook 的典型场景|
|犯错后自动把教训写进 CLAUDE.md|**Skill**（`/distill-lesson`）|需要理解"刚才的错是什么、该怎么抽象成规则"，是推理任务|
|收工时写日报 + 更新项目笔记|**Skill**（`/wrap-up`）|结构化流程但需要对内容判断|
|开新项目三步仪式|**Skill**（`/new-project`）|典型的工作流 skill|
## 建议的落地顺序

1. **先用 Hook 把最机械的两件事自动化**：SessionStart 恢复上下文 + PostToolUse 跑验证 —— 这俩收益最高，成本最低
2. **再写 3 个 Skill**：`/new-project`、`/distill-lesson`、`/wrap-up` —— 把重复对话固化成命令
3. **CLAUDE.md 保持精简** —— 只留跨项目的通用规则

---

要我帮你：

- **A.** 先写 SessionStart Hook（自动读 CLAUDE.md + 项目笔记 + git status）
- **B.** 先写 `/new-project` Skill（把你现在「三步仪式」固化）
- **C.** 列出完整的 Hook + Skill 清单供你挑选实现顺序